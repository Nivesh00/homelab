---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: monitoring-grafana
spec:
  parentRefs:
    - name: traefik
      sectionName: monitoring-grafana
      kind: Gateway

  hostnames:
    - monitoring-curepipe.duckdns.org

  rules:
     - matches:
        - path:
            type: PathPrefix
            value: /

       backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80